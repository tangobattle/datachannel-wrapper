[package]
name = "datachannel-wrapper"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
libdatachannel = { git = "https://github.com/tangobattle/libdatachannel-rs", features = ["vendored"] }
log = "0.4"
tokio = { version = "1", features = ["full"] }
